export interface Explore {
  img: string;
  location: string;
  distance: string;
}

export interface Card {
  img: string;
  title: string;
}

export interface LCard {
  img: string;
  title: string;
  description: string;
  buttonText: string;
}

export interface IFCard {
  img: string;
  location: string;
  title: string;
  description: string;
  star: string;
  price: string;
  total: string;
}
