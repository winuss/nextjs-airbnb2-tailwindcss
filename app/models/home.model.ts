export interface Explore {
  img: string;
  location: string;
  distance: string;
}

export interface Card {
  img: string;
  title: string;
}

export interface LCard {
  img: string;
  title: string;
  description: string;
  buttonText: string;
}
